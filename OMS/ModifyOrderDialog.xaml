<Window x:Class="ATMML.ModifyOrderDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Modify Order"
        Height="300" Width="420"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#1E1E1E">
    <Grid Margin="14">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="120"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Text="Order:"        Foreground="#9A9A9A" Grid.Row="0" Grid.Column="0" Margin="0,0,0,8"/>
        <TextBlock x:Name="TxtSummary"  Foreground="#DCDCDC" Grid.Row="0" Grid.Column="1"
                   Margin="0,0,0,8" TextWrapping="Wrap"/>

        <TextBlock Text="Filled:"       Foreground="#9A9A9A" Grid.Row="1" Grid.Column="0" Margin="0,0,0,8"/>
        <TextBlock x:Name="TxtFilled"   Foreground="#DCDCDC" Grid.Row="1" Grid.Column="1" Margin="0,0,0,8"/>

        <TextBlock Text="New Qty:"      Foreground="#DCDCDC" Grid.Row="2" Grid.Column="0"
                   Margin="0,0,0,8" VerticalAlignment="Center"/>
        <TextBox  x:Name="TxtQty"       Grid.Row="2" Grid.Column="1" Margin="0,0,0,8"
                  Background="#2D2D30" Foreground="#DCDCDC" BorderBrush="#3F3F46"
                  Padding="6,4" FontSize="13"/>

        <TextBlock Text="New Limit ($):" Foreground="#DCDCDC" Grid.Row="3" Grid.Column="0"
                   Margin="0,0,0,8" VerticalAlignment="Center"/>
        <TextBox  x:Name="TxtLimit"      Grid.Row="3" Grid.Column="1" Margin="0,0,0,8"
                  Background="#2D2D30" Foreground="#DCDCDC" BorderBrush="#3F3F46"
                  Padding="6,4" FontSize="13"/>

        <TextBlock x:Name="TxtError" Grid.Row="4" Grid.ColumnSpan="2"
                   Foreground="#FF6464" Margin="0,4,0,0" TextWrapping="Wrap"/>

        <StackPanel Grid.Row="6" Grid.ColumnSpan="2"
                    Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Cancel" Width="80" Margin="0,0,8,0" Click="BtnCancel_Click"
                    Background="#2D2D30" Foreground="#DCDCDC" BorderBrush="#3F3F46" Padding="8,4"/>
            <Button Content="OK"     Width="80" Click="BtnOk_Click" IsDefault="True"
                    Background="#0E5A8A" Foreground="White"   BorderBrush="#0E5A8A" Padding="8,4"/>
        </StackPanel>
    </Grid>
</Window>
