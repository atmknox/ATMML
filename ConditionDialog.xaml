<Window x:Class="ATMML.ConditionDialog"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" 
             d:DesignWidth="300"
             Height="400">

    <Grid  Background="Black">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>
        
        <Grid.RowDefinitions>
            <RowDefinition Height="30" />
            <RowDefinition />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2" Orientation="Horizontal">
            <Label Content="ADD or EDIT CHART CONDITIONS"  Padding="10,0" Background="Transparent" Foreground="White" HorizontalAlignment="Left" FontFamily="Helvetica Neue"  FontSize="11" VerticalAlignment="Center" Width="300" />
        </StackPanel>

        <StackPanel Margin="0,0,4,0"  Grid.Row="1" Grid.Column="0"  Background="Black" >
            <RadioButton GroupName="Condition" VerticalAlignment="Center" Background="#FF00CCFF" Foreground="White" FontSize="10" HorizontalAlignment="Left" Padding="5,-0,0,0" Margin="5,5,0,0"  Checked="Condition_Checked" Content="Condition 1" IsChecked="True" />
            <RadioButton GroupName="Condition"  Background="#FF00CCFF" Foreground="White" FontSize="10"  HorizontalAlignment="Left" Padding="5,-0,0,0" Margin="5,5,0,0"   Checked="Condition_Checked" Content="Condition 2" />
            <RadioButton GroupName="Condition"  Background="#FF00CCFF" Foreground="White"  FontSize="10" HorizontalAlignment="Left" Padding="5,0,0,0" Margin="5,5,0,0"  Checked="Condition_Checked" Content="Condition 3" />
            <RadioButton GroupName="Condition"  Background="#FF00CCFF" Foreground="White" FontSize="10"  HorizontalAlignment="Left" Padding="5,0,0,0" Margin="5,5,0,0"  Checked="Condition_Checked" Content="Condition 4"/>
            <RadioButton GroupName="Condition"  Background="#FF00CCFF" Foreground="White" FontSize="10"  HorizontalAlignment="Left" Padding="5,0,0,0" Margin="5,5,0,0"  Checked="Condition_Checked" Content="Condition 5" />
            <RadioButton GroupName="Condition"  Background="#FF00CCFF" Foreground="White" FontSize="10"  HorizontalAlignment="Left" Padding="5,0,0,0" Margin="5,5,0,0" Checked="Condition_Checked" Content="Condition 6"/>

            <RadioButton Name="AllBars"  Background="#FF00CCFF" Foreground="White" FontSize="10"  HorizontalAlignment="Left" Padding="5,0,0,0" Margin="5,50,0,0" GroupName="ConditionType" Checked="ConditionType_Checked" Content="All Bars" IsChecked="True"/>
            <RadioButton Name="LastBar"  Background="#FF00CCFF" Foreground="White" FontSize="10"  HorizontalAlignment="Left" Padding="5,0,0,0" Margin="5,5,0,0"  GroupName="ConditionType" Checked="ConditionType_Checked" Content="Last Bar"  />
        </StackPanel>

        <TreeView Name="ConditionTree" Grid.Row="1" Grid.Column="1" Margin="5" Background="Black" Foreground="White" FontWeight="Normal" BorderThickness="2" BorderBrush="#FF124B72"></TreeView>

        <StackPanel Grid.Row="2" Grid.Column="0" Grid.ColumnSpan =" 2" Orientation="Horizontal">
            <Button Content="Save" Width="100" Padding="0" Margin="2" IsDefault="True" Click="Save_Click" Cursor="Hand" FontSize="11"></Button>
            <Button Content="Cancel" Width="100" Padding="0" Margin="2" IsCancel="True" Click="Cancel_Click" IsDefault="False" Cursor="Hand" FontSize="11"></Button>
        </StackPanel>
        
    </Grid>
</Window>
