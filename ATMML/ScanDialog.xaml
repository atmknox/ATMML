<Window x:Class="ATMML.ScanDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
        xmlns:Themes="clr-namespace:Microsoft.Windows.Themes;assembly=PresentationFramework.Aero2" 
        xmlns:local ="clr-namespace:ATMML"
        mc:Ignorable="d"
        Height="448" Width="725"
        MinHeight="448" MinWidth="725"
        IsEnabled="True"
        FontSize="11"
        UseLayoutRounding="True"
        SnapsToDevicePixels="True"
        Background="Black"
        AllowsTransparency="False"
        Loaded="Window_Loaded">
    

    <Window.Resources>
        <Style x:Key="FocusVisual">
            <Setter Property="Control.Template">
                <Setter.Value>
                    <ControlTemplate>
                        <Rectangle Margin="2" SnapsToDevicePixels="true" Stroke="{DynamicResource {x:Static SystemColors.ControlTextBrushKey}}" StrokeThickness="1" StrokeDashArray="1 2"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <SolidColorBrush x:Key="ComboBox.Static.Background" Color="Black"/>
        <SolidColorBrush x:Key="ComboBox.Static.Border" Color="Black"/>
        <SolidColorBrush x:Key="ComboBox.Static.Editable.Background" Color="Black"/>
        <SolidColorBrush x:Key="ComboBox.Static.Editable.Border" Color="Black"/>
        <SolidColorBrush x:Key="ComboBox.Static.Editable.Button.Background" Color="Transparent"/>
        <SolidColorBrush x:Key="ComboBox.Static.Editable.Button.Border" Color="Transparent"/>
        <SolidColorBrush x:Key="ComboBox.MouseOver.Glyph" Color="White"/>
        <SolidColorBrush x:Key="ComboBox.MouseOver.Background" Color="Black"/>
        <SolidColorBrush x:Key="ComboBox.MouseOver.Border" Color="Black"/>
        <SolidColorBrush x:Key="ComboBox.MouseOver.Editable.Background" Color="Black"/>
        <SolidColorBrush x:Key="ComboBox.MouseOver.Editable.Border" Color="Black"/>
        <SolidColorBrush x:Key="ComboBox.MouseOver.Editable.Button.Background" Color="Black"/>
        <SolidColorBrush x:Key="ComboBox.MouseOver.Editable.Button.Border" Color="Black"/>
        <SolidColorBrush x:Key="ComboBox.Pressed.Glyph" Color="White"/>
        <SolidColorBrush x:Key="ComboBox.Pressed.Background" Color="Black" />
        <SolidColorBrush x:Key="ComboBox.Pressed.Border" Color="Black"/>
        <SolidColorBrush x:Key="ComboBox.Pressed.Editable.Background" Color="Black"/>
        <SolidColorBrush x:Key="ComboBox.Pressed.Editable.Border" Color="Black"/>
        <SolidColorBrush x:Key="ComboBox.Pressed.Editable.Button.Background" Color="Black"/>
        <SolidColorBrush x:Key="ComboBox.Pressed.Editable.Button.Border" Color="Black"/>
        <SolidColorBrush x:Key="ComboBox.Disabled.Glyph" Color="White"/>
        <SolidColorBrush x:Key="ComboBox.Disabled.Background" Color="Black"/>
        <SolidColorBrush x:Key="ComboBox.Disabled.Border" Color="Black"/>
        <SolidColorBrush x:Key="ComboBox.Disabled.Editable.Background" Color="Black"/>
        <SolidColorBrush x:Key="ComboBox.Disabled.Editable.Border" Color="Black"/>
        <SolidColorBrush x:Key="ComboBox.Disabled.Editable.Button.Background" Color="Transparent"/>
        <SolidColorBrush x:Key="ComboBox.Disabled.Editable.Button.Border" Color="Transparent"/>
        <SolidColorBrush x:Key="ComboBox.Static.Glyph" Color="White"/>

        <Style x:Key="ComboBoxToggleButton" TargetType="{x:Type ToggleButton}">
            <Setter Property="OverridesDefaultStyle" Value="true"/>
            <Setter Property="IsTabStop" Value="false"/>
            <Setter Property="Focusable" Value="false"/>
            <Setter Property="ClickMode" Value="Press"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type ToggleButton}">
                        <Border x:Name="templateRoot" BorderBrush="{StaticResource ComboBox.Static.Border}" BorderThickness="{TemplateBinding BorderThickness}" Background="{StaticResource ComboBox.Static.Background}" SnapsToDevicePixels="true">
                            <Border x:Name="splitBorder" BorderBrush="Transparent" BorderThickness="1" HorizontalAlignment="Right" Margin="0" SnapsToDevicePixels="true" Width="{DynamicResource {x:Static SystemParameters.VerticalScrollBarWidthKey}}">
                                <Path x:Name="arrow" Data="F1 M 0,0 L 2.667,2.66665 L 5.3334,0 L 5.3334,-1.78168 L 2.6667,0.88501 L0,-1.78168 L0,0 Z" Fill="{StaticResource ComboBox.Static.Glyph}" HorizontalAlignment="Center" Margin="0" VerticalAlignment="Center"/>
                            </Border>
                        </Border>
                        <ControlTemplate.Triggers>
                            <MultiDataTrigger>
                                <MultiDataTrigger.Conditions>
                                    <Condition Binding="{Binding IsEditable, RelativeSource={RelativeSource AncestorType={x:Type ComboBox}}}" Value="true"/>
                                    <Condition Binding="{Binding IsMouseOver, RelativeSource={RelativeSource Self}}" Value="false"/>
                                    <Condition Binding="{Binding IsPressed, RelativeSource={RelativeSource Self}}" Value="false"/>
                                    <Condition Binding="{Binding IsEnabled, RelativeSource={RelativeSource Self}}" Value="true"/>
                                </MultiDataTrigger.Conditions>
                                <Setter Property="Background" TargetName="templateRoot" Value="{StaticResource ComboBox.Static.Editable.Background}"/>
                                <Setter Property="BorderBrush" TargetName="templateRoot" Value="{StaticResource ComboBox.Static.Editable.Border}"/>
                                <Setter Property="Background" TargetName="splitBorder" Value="{StaticResource ComboBox.Static.Editable.Button.Background}"/>
                                <Setter Property="BorderBrush" TargetName="splitBorder" Value="{StaticResource ComboBox.Static.Editable.Button.Border}"/>
                            </MultiDataTrigger>
                            <Trigger Property="IsMouseOver" Value="true">
                                <Setter Property="Fill" TargetName="arrow" Value="{StaticResource ComboBox.MouseOver.Glyph}"/>
                            </Trigger>
                            <MultiDataTrigger>
                                <MultiDataTrigger.Conditions>
                                    <Condition Binding="{Binding IsMouseOver, RelativeSource={RelativeSource Self}}" Value="true"/>
                                    <Condition Binding="{Binding IsEditable, RelativeSource={RelativeSource AncestorType={x:Type ComboBox}}}" Value="false"/>
                                </MultiDataTrigger.Conditions>
                                <Setter Property="Background" TargetName="templateRoot" Value="{StaticResource ComboBox.MouseOver.Background}"/>
                                <Setter Property="BorderBrush" TargetName="templateRoot" Value="{StaticResource ComboBox.MouseOver.Border}"/>
                            </MultiDataTrigger>
                            <MultiDataTrigger>
                                <MultiDataTrigger.Conditions>
                                    <Condition Binding="{Binding IsMouseOver, RelativeSource={RelativeSource Self}}" Value="true"/>
                                    <Condition Binding="{Binding IsEditable, RelativeSource={RelativeSource AncestorType={x:Type ComboBox}}}" Value="true"/>
                                </MultiDataTrigger.Conditions>
                                <Setter Property="Background" TargetName="templateRoot" Value="{StaticResource ComboBox.MouseOver.Editable.Background}"/>
                                <Setter Property="BorderBrush" TargetName="templateRoot" Value="{StaticResource ComboBox.MouseOver.Editable.Border}"/>
                                <Setter Property="Background" TargetName="splitBorder" Value="{StaticResource ComboBox.MouseOver.Editable.Button.Background}"/>
                                <Setter Property="BorderBrush" TargetName="splitBorder" Value="{StaticResource ComboBox.MouseOver.Editable.Button.Border}"/>
                            </MultiDataTrigger>
                            <Trigger Property="IsPressed" Value="true">
                                <Setter Property="Fill" TargetName="arrow" Value="{StaticResource ComboBox.Pressed.Glyph}"/>
                            </Trigger>
                            <MultiDataTrigger>
                                <MultiDataTrigger.Conditions>
                                    <Condition Binding="{Binding IsPressed, RelativeSource={RelativeSource Self}}" Value="true"/>
                                    <Condition Binding="{Binding IsEditable, RelativeSource={RelativeSource AncestorType={x:Type ComboBox}}}" Value="false"/>
                                </MultiDataTrigger.Conditions>
                                <Setter Property="Background" TargetName="templateRoot" Value="{StaticResource ComboBox.Pressed.Background}"/>
                                <Setter Property="BorderBrush" TargetName="templateRoot" Value="{StaticResource ComboBox.Pressed.Border}"/>
                            </MultiDataTrigger>
                            <MultiDataTrigger>
                                <MultiDataTrigger.Conditions>
                                    <Condition Binding="{Binding IsPressed, RelativeSource={RelativeSource Self}}" Value="true"/>
                                    <Condition Binding="{Binding IsEditable, RelativeSource={RelativeSource AncestorType={x:Type ComboBox}}}" Value="true"/>
                                </MultiDataTrigger.Conditions>
                                <Setter Property="Background" TargetName="templateRoot" Value="{StaticResource ComboBox.Pressed.Editable.Background}"/>
                                <Setter Property="BorderBrush" TargetName="templateRoot" Value="{StaticResource ComboBox.Pressed.Editable.Border}"/>
                                <Setter Property="Background" TargetName="splitBorder" Value="{StaticResource ComboBox.Pressed.Editable.Button.Background}"/>
                                <Setter Property="BorderBrush" TargetName="splitBorder" Value="{StaticResource ComboBox.Pressed.Editable.Button.Border}"/>
                            </MultiDataTrigger>
                            <Trigger Property="IsEnabled" Value="false">
                                <Setter Property="Fill" TargetName="arrow" Value="{StaticResource ComboBox.Disabled.Glyph}"/>
                            </Trigger>
                            <MultiDataTrigger>
                                <MultiDataTrigger.Conditions>
                                    <Condition Binding="{Binding IsEnabled, RelativeSource={RelativeSource Self}}" Value="false"/>
                                    <Condition Binding="{Binding IsEditable, RelativeSource={RelativeSource AncestorType={x:Type ComboBox}}}" Value="false"/>
                                </MultiDataTrigger.Conditions>
                                <Setter Property="Background" TargetName="templateRoot" Value="{StaticResource ComboBox.Disabled.Background}"/>
                                <Setter Property="BorderBrush" TargetName="templateRoot" Value="{StaticResource ComboBox.Disabled.Border}"/>
                            </MultiDataTrigger>
                            <MultiDataTrigger>
                                <MultiDataTrigger.Conditions>
                                    <Condition Binding="{Binding IsEnabled, RelativeSource={RelativeSource Self}}" Value="false"/>
                                    <Condition Binding="{Binding IsEditable, RelativeSource={RelativeSource AncestorType={x:Type ComboBox}}}" Value="true"/>
                                </MultiDataTrigger.Conditions>
                                <Setter Property="Background" TargetName="templateRoot" Value="{StaticResource ComboBox.Disabled.Editable.Background}"/>
                                <Setter Property="BorderBrush" TargetName="templateRoot" Value="{StaticResource ComboBox.Disabled.Editable.Border}"/>
                                <Setter Property="Background" TargetName="splitBorder" Value="{StaticResource ComboBox.Disabled.Editable.Button.Background}"/>
                                <Setter Property="BorderBrush" TargetName="splitBorder" Value="{StaticResource ComboBox.Disabled.Editable.Button.Border}"/>
                            </MultiDataTrigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <ControlTemplate x:Key="ComboBoxTemplate" TargetType="{x:Type ComboBox}">
            <Grid x:Name="templateRoot" SnapsToDevicePixels="true">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition MinWidth="{DynamicResource {x:Static SystemParameters.VerticalScrollBarWidthKey}}" Width="0"/>
                </Grid.ColumnDefinitions>
                <Popup x:Name="PART_Popup" AllowsTransparency="true" Grid.ColumnSpan="2" IsOpen="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}" Margin="1" PopupAnimation="{DynamicResource {x:Static SystemParameters.ComboBoxPopupAnimationKey}}" Placement="Bottom">
                    <Themes:SystemDropShadowChrome x:Name="shadow" Color="Transparent" MaxHeight="{TemplateBinding MaxDropDownHeight}" MinWidth="{Binding ActualWidth, ElementName=templateRoot}">
                        <Border x:Name="dropDownBorder" BorderBrush="{DynamicResource {x:Static SystemColors.WindowFrameBrushKey}}" BorderThickness="1" Background="Black">
                            <ScrollViewer x:Name="DropDownScrollViewer">
                                <Grid x:Name="grid" RenderOptions.ClearTypeHint="Enabled">
                                    <Canvas x:Name="canvas" HorizontalAlignment="Left" Height="0" VerticalAlignment="Top" Width="0">
                                        <Rectangle x:Name="opaqueRect" Fill="{Binding Background, ElementName=dropDownBorder}" Height="{Binding ActualHeight, ElementName=dropDownBorder}" Width="{Binding ActualWidth, ElementName=dropDownBorder}"/>
                                    </Canvas>
                                    <ItemsPresenter x:Name="ItemsPresenter" KeyboardNavigation.DirectionalNavigation="Contained" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
                                </Grid>
                            </ScrollViewer>
                        </Border>
                    </Themes:SystemDropShadowChrome>
                </Popup>
                <ToggleButton x:Name="toggleButton" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Background="{TemplateBinding Background}" Grid.ColumnSpan="2" IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}" Style="{StaticResource ComboBoxToggleButton}"/>
                <ContentPresenter x:Name="contentPresenter" ContentTemplate="{TemplateBinding SelectionBoxItemTemplate}" ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}" Content="{TemplateBinding SelectionBoxItem}" ContentStringFormat="{TemplateBinding SelectionBoxItemStringFormat}" HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" IsHitTestVisible="false" Margin="{TemplateBinding Padding}" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
            </Grid>
            <ControlTemplate.Triggers>
                <Trigger Property="HasDropShadow" SourceName="PART_Popup" Value="true">
                    <Setter Property="Margin" TargetName="shadow" Value="0,0,5,5"/>
                    <Setter Property="Color" TargetName="shadow" Value="#71000000"/>
                </Trigger>
                <Trigger Property="HasItems" Value="false">
                    <Setter Property="Height" TargetName="dropDownBorder" Value="95"/>
                </Trigger>
                <MultiTrigger>
                    <MultiTrigger.Conditions>
                        <Condition Property="IsGrouping" Value="true"/>
                        <Condition Property="VirtualizingPanel.IsVirtualizingWhenGrouping" Value="false"/>
                    </MultiTrigger.Conditions>
                    <Setter Property="ScrollViewer.CanContentScroll" Value="false"/>
                </MultiTrigger>
                <Trigger Property="ScrollViewer.CanContentScroll" SourceName="DropDownScrollViewer" Value="false">
                    <Setter Property="Canvas.Top" TargetName="opaqueRect" Value="{Binding VerticalOffset, ElementName=DropDownScrollViewer}"/>
                    <Setter Property="Canvas.Left" TargetName="opaqueRect" Value="{Binding HorizontalOffset, ElementName=DropDownScrollViewer}"/>
                </Trigger>
            </ControlTemplate.Triggers>
        </ControlTemplate>

        <SolidColorBrush x:Key="TextBox.Static.Background" Color="Black"/>

        <Style x:Key="ComboBoxEditableTextBox" TargetType="{x:Type TextBox}">
            <Setter Property="OverridesDefaultStyle" Value="true"/>
            <Setter Property="AllowDrop" Value="true"/>
            <Setter Property="MinWidth" Value="0"/>
            <Setter Property="MinHeight" Value="0"/>
            <Setter Property="FocusVisualStyle" Value="{x:Null}"/>
            <Setter Property="ScrollViewer.PanningMode" Value="VerticalFirst"/>
            <Setter Property="Stylus.IsFlicksEnabled" Value="False"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="{x:Type TextBox}">
                        <ScrollViewer x:Name="PART_ContentHost" Background="Transparent" Focusable="false" HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Hidden"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <ControlTemplate x:Key="ComboBoxEditableTemplate" TargetType="{x:Type ComboBox}">
            <Grid x:Name="templateRoot" SnapsToDevicePixels="true">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition MinWidth="{DynamicResource {x:Static SystemParameters.VerticalScrollBarWidthKey}}" Width="0"/>
                </Grid.ColumnDefinitions>
                <Popup x:Name="PART_Popup" AllowsTransparency="true" Grid.ColumnSpan="2" IsOpen="{Binding IsDropDownOpen, RelativeSource={RelativeSource TemplatedParent}}" PopupAnimation="{DynamicResource {x:Static SystemParameters.ComboBoxPopupAnimationKey}}" Placement="Bottom">
                    <Themes:SystemDropShadowChrome x:Name="shadow" Color="Transparent" MaxHeight="{TemplateBinding MaxDropDownHeight}" MinWidth="{Binding ActualWidth, ElementName=templateRoot}">
                        <Border x:Name="dropDownBorder" BorderBrush="{DynamicResource {x:Static SystemColors.WindowFrameBrushKey}}" BorderThickness="1" Background="Black">
                            <ScrollViewer x:Name="DropDownScrollViewer">
                                <Grid x:Name="grid" RenderOptions.ClearTypeHint="Enabled">
                                    <Canvas x:Name="canvas" HorizontalAlignment="Left" Height="0" VerticalAlignment="Top" Width="0">
                                        <Rectangle x:Name="opaqueRect" Fill="{Binding Background, ElementName=dropDownBorder}" Height="{Binding ActualHeight, ElementName=dropDownBorder}" Width="{Binding ActualWidth, ElementName=dropDownBorder}"/>
                                    </Canvas>
                                    <ItemsPresenter x:Name="ItemsPresenter" KeyboardNavigation.DirectionalNavigation="Contained" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
                                </Grid>
                            </ScrollViewer>
                        </Border>
                    </Themes:SystemDropShadowChrome>
                </Popup>
                <ToggleButton x:Name="toggleButton" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Background="{TemplateBinding Background}" Grid.ColumnSpan="2" IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}" Style="{StaticResource ComboBoxToggleButton}"/>
                <Border x:Name="border" Background="{StaticResource TextBox.Static.Background}" Margin="{TemplateBinding BorderThickness}">
                    <TextBox x:Name="PART_EditableTextBox" HorizontalContentAlignment="{TemplateBinding HorizontalContentAlignment}" IsReadOnly="{Binding IsReadOnly, RelativeSource={RelativeSource TemplatedParent}}" Margin="{TemplateBinding Padding}" Style="{StaticResource ComboBoxEditableTextBox}" VerticalContentAlignment="{TemplateBinding VerticalContentAlignment}"/>
                </Border>
            </Grid>
            <ControlTemplate.Triggers>
                <Trigger Property="IsEnabled" Value="false">
                    <Setter Property="Opacity" TargetName="border" Value="0.56"/>
                </Trigger>
                <Trigger Property="IsKeyboardFocusWithin" Value="true">
                    <Setter Property="Foreground" Value="Black"/>
                </Trigger>
                <Trigger Property="HasDropShadow" SourceName="PART_Popup" Value="true">
                    <Setter Property="Margin" TargetName="shadow" Value="0,0,5,5"/>
                    <Setter Property="Color" TargetName="shadow" Value="#71000000"/>
                </Trigger>
                <Trigger Property="HasItems" Value="false">
                    <Setter Property="Height" TargetName="dropDownBorder" Value="95"/>
                </Trigger>
                <MultiTrigger>
                    <MultiTrigger.Conditions>
                        <Condition Property="IsGrouping" Value="true"/>
                        <Condition Property="VirtualizingPanel.IsVirtualizingWhenGrouping" Value="false"/>
                    </MultiTrigger.Conditions>
                    <Setter Property="ScrollViewer.CanContentScroll" Value="false"/>
                </MultiTrigger>
                <Trigger Property="ScrollViewer.CanContentScroll" SourceName="DropDownScrollViewer" Value="false">
                    <Setter Property="Canvas.Top" TargetName="opaqueRect" Value="{Binding VerticalOffset, ElementName=DropDownScrollViewer}"/>
                    <Setter Property="Canvas.Left" TargetName="opaqueRect" Value="{Binding HorizontalOffset, ElementName=DropDownScrollViewer}"/>
                </Trigger>
            </ControlTemplate.Triggers>
        </ControlTemplate>

        <Style x:Key="ComboBoxStyle1" TargetType="{x:Type ComboBox}">
            <Setter Property="FocusVisualStyle" Value="{StaticResource FocusVisual}"/>
            <Setter Property="Background" Value="{StaticResource ComboBox.Static.Background}"/>
            <Setter Property="BorderBrush" Value="{StaticResource ComboBox.Static.Border}"/>
            <Setter Property="Foreground" Value="{DynamicResource {x:Static SystemColors.WindowTextBrushKey}}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Auto"/>
            <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto"/>
            <Setter Property="Padding" Value="6,3,5,3"/>
            <Setter Property="ScrollViewer.CanContentScroll" Value="true"/>
            <Setter Property="ScrollViewer.PanningMode" Value="Both"/>
            <Setter Property="Stylus.IsFlicksEnabled" Value="False"/>
            <Setter Property="Template" Value="{StaticResource ComboBoxTemplate}"/>
            <Style.Triggers>
                <Trigger Property="IsEditable" Value="true">
                    <Setter Property="IsTabStop" Value="false"/>
                    <Setter Property="Padding" Value="2"/>
                    <Setter Property="Template" Value="{StaticResource ComboBoxEditableTemplate}"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <!-- NEW: Wrap content in a Viewbox to scale fonts & controls uniformly -->
    <Border Background="Black">
    <Viewbox Stretch="UniformToFill" StretchDirection="Both"> 
        <!-- Design-time size for the layout inside the Viewbox -->
        <Grid Background="Black" Width="700" Height="360">
            <Grid.RowDefinitions>
                <RowDefinition Height="24" />
                <RowDefinition Height="24" />
                <RowDefinition />
                <RowDefinition Height="10" />
                <RowDefinition Height="24" />
                <RowDefinition Height="10" />
                <RowDefinition Height="24" />
                <RowDefinition Height="10" />
                <RowDefinition Height="24" />
                <RowDefinition Height="10" />
                <RowDefinition Height="24" />
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" Orientation="Horizontal">
                <Label Name="Title1" Content="ATM SCAN SETUP"  Padding="10,0" Background="Transparent" Foreground="#a0a0a0" HorizontalAlignment="Left" FontFamily="Helvetica Neue"  FontSize="11" VerticalAlignment="Center" Width="300" />
            </StackPanel>

            <StackPanel Grid.Row="1" Orientation="Horizontal">
                <Label Name="Step1" Content="Step 1:  Select Your Security List"  Padding="10,0" Background="Transparent" Foreground="#a0a0a0" HorizontalAlignment="Left" FontFamily="Helvetica Neue"  FontSize="11" VerticalAlignment="Center" FontWeight="Normal" Width="300" />
                <Label Name="Step2" Content="Step 2:  Select Your ATM Conditions"  Padding="10,0" Background="Transparent" Foreground="#a0a0a0" HorizontalAlignment="Left" FontFamily="Helvetica Neue"  FontSize="11" VerticalAlignment="Center" FontWeight="Normal" Width="397" />
            </StackPanel>

            <Grid Grid.Row="2" Background="Black">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="300" />
                    <ColumnDefinition Width="425" />
                </Grid.ColumnDefinitions>

                <TreeView Grid.Column="0" Name="PortfolioTree"  FontWeight="Normal" Background="Black" Foreground="White" Margin="5,0,5,5"  BorderThickness="1"  BorderBrush="#FF124B72" />

                <TreeView Grid.Column="1" Name="ConditionTree" Loaded="ConditionTree_Loaded"  Margin="5,0,5,5"  Background="Black" FontSize="10" Foreground="White" FontWeight="Normal" BorderThickness="1"  BorderBrush="#FF124B72"   VerticalAlignment="Stretch" Height="Auto" VirtualizingPanel.IsVirtualizing="True">
                    <TreeView.Resources>
                        <SolidColorBrush x:Key="{x:Static SystemColors.HighlightBrushKey}" Color="#FF124b72" />
                        <SolidColorBrush x:Key="{x:Static SystemColors.HighlightTextBrushKey}" Color="White" />
                        <SolidColorBrush x:Key="{x:Static SystemColors.InactiveSelectionHighlightBrushKey}" Color="Transparent" />
                        <SolidColorBrush x:Key="{x:Static SystemColors.InactiveSelectionHighlightTextBrushKey}" Color="White" />

                        <HierarchicalDataTemplate DataType = "{x:Type local:TreeNode}" ItemsSource = "{Binding Path=Children}">
                            <StackPanel>
                                <TextBlock   FontSize="10" Foreground="White" Background="Black" FontWeight="Normal" Text="{Binding Path=Name, Mode=OneWay}" Width="220" Visibility="{Binding Path=IsGroup, Mode=OneWay, Converter={StaticResource BoolToVisConv}}"/>
                                <TextBlock   FontSize="10" Foreground="White" Background="Black" FontWeight="Normal" Text="{Binding Path=Name, Mode=OneWay}" Width="220" Visibility="{Binding Path=IsIndicator, Mode=OneWay, Converter={StaticResource BoolToVisConv}}"/>

                                <StackPanel Orientation="Horizontal" Margin="4,0" Visibility="{Binding Path=IsFundamental, Mode=OneWay, Converter={StaticResource BoolToVisConv}}">
                                    <CheckBox Click="CheckBox_Click" IsChecked="{Binding Path=IsChecked}"  Margin="2" />
                                    <TextBlock FontSize="10" Foreground="White" Background="Black" FontWeight="Normal"  Text="{Binding Path=Name, Mode=OneWay}" Width="220"/>
                                    <ComboBox Style="{DynamicResource ComboBoxStyle1}" SelectionChanged="ComboBox_SelectionChanged" Foreground="White" Background="Black" SelectedValue="{Binding Path=Relationship, Mode=TwoWay}" SelectedValuePath="Content" Padding="2,0" VerticalContentAlignment="Top" Height ="18"  Width="40">
                                        <ComboBoxItem Content="&lt;"/>
                                        <ComboBoxItem Content="&lt;="/>
                                        <ComboBoxItem Content="&gt;"/>
                                        <ComboBoxItem Content="&gt;="/>

                                        <ComboBox.Resources>
                                            <SolidColorBrush x:Key="{x:Static SystemColors.WindowBrushKey}" Color="Black" />
                                            <SolidColorBrush x:Key="{x:Static SystemColors.HighlightBrushKey}" Color="Transparent" />

                                            <Style TargetType="TextBlock">
                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsSelected, RelativeSource={RelativeSource AncestorType=ComboBoxItem}}" Value="True">
                                                        <Setter Property="Background" Value="Black" />
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </ComboBox.Resources>
                                    </ComboBox>
                                    <TextBox FontSize="10" Foreground="White" Background="Black" FontWeight="Normal" TextChanged="TextBox_TextChanged" Text="{Binding Path=Value, Mode=TwoWay}" Width="40"/>
                                </StackPanel>

                                <StackPanel Orientation="Horizontal" Visibility="{Binding Path=IsCondition, Mode=OneWay, Converter={StaticResource BoolToVisConv}}">
                                    <CheckBox Click="CheckBox_Click" IsChecked="{Binding Path=IsChecked}" Margin="2" />
                                    <TextBlock  FontSize="10" Foreground="White" Background="Black" FontWeight="Normal"  Text="{Binding Path=Interval, Mode=OneWay}" Width="220"/>
                                </StackPanel>

                                <StackPanel Orientation="Horizontal" Visibility="{Binding Path=IsIndicatorInterval, Mode=OneWay, Converter={StaticResource BoolToVisConv}}">
                                    <CheckBox Click="CheckBox_Click" IsChecked="{Binding Path=IsChecked}" Margin="2" />
                                    <TextBlock  FontSize="10" Foreground="White" Background="Black" FontWeight="Normal"  Text="{Binding Path=Interval, Mode=OneWay}" Width="220"/>
                                </StackPanel>
                            </StackPanel>
                        </HierarchicalDataTemplate>

                        <Style TargetType="TreeViewItem">
                            <Setter Property="IsSelected" Value="{Binding Path=IsSelected}"/>
                            <Setter Property="IsExpanded" Value="{Binding Path=IsExpanded}"/>
                        </Style>
                    </TreeView.Resources>
                </TreeView>
            </Grid>

            <StackPanel Name="Row3" Grid.Row="3" Orientation="Horizontal" Visibility="Collapsed">
                <Label Name="Step3" Content="Step 3:  Choose your results text color.  Left:"  Padding="10,0" Background="Transparent" FontSize="11" Foreground="#a0a0a0" HorizontalAlignment="Left" FontFamily="Helvetica Neue"  VerticalAlignment="Center" FontWeight="Normal" />
                <Label Content="Right:"  Padding="10,0" Background="Transparent" Foreground="White" HorizontalAlignment="Left" FontFamily="Helvetica Neue"  FontSize="11" VerticalAlignment="Center" FontWeight="Normal" />
            </StackPanel>

            <StackPanel Name="Row5" Grid.Row="4" Orientation="Horizontal" Visibility="Visible">
                <Label  Content="Step 3:  Your Scan Name" Margin="5,0,0,0"  Background="Transparent" Foreground="#a0a0a0" HorizontalAlignment="Left" FontFamily="Helvetica Neue"  FontSize="11" VerticalAlignment="Center" FontWeight="Normal" Width="135" />
                <TextBox Name="ScanTitle" Foreground="White" Width="490" TextChanged="ScanTitle_TextChanged"  BorderThickness="1" VerticalContentAlignment="Center" BorderBrush="#FF124B72" Background="Black"/>
                <Label Content="Default Title:"  Padding="5,0" Background="Transparent" Foreground="#a0a0a0" HorizontalAlignment="Left" FontFamily="Helvetica Neue"  FontSize="11" VerticalAlignment="Center" FontWeight="Normal" Visibility="Visible"/>
                <CheckBox Name="DefaultTitleCheckbox" VerticalAlignment="Center"  Background="White"  Foreground="White" Checked="DefaultTitleCheckbox_Checked" Visibility="Visible" />
                <Label Content="Alert Text:"  Padding="5,0" Background="Transparent" Foreground="White" HorizontalAlignment="Left" FontFamily="Helvetica Neue"  FontSize="11" VerticalAlignment="Center" FontWeight="Normal" Visibility="Collapsed" />
            </StackPanel>

            <StackPanel Name="Row4" Grid.Row="6" Orientation="Horizontal" Visibility="Visible">
                <Label Content="Step 4:  Turn Notification ON"  Margin="5,0,0,0" Background="Transparent" Foreground="#a0a0a0" HorizontalAlignment="Left" FontFamily="Helvetica Neue"  FontSize="11" VerticalAlignment="Center" FontWeight="Normal" />
                <CheckBox Name="NotificationCheckbox" Margin="20,0,0,0" Background="White" Foreground="White" Cursor="Hand" VerticalAlignment="Center"/>
            </StackPanel>

            <StackPanel Grid.Row="8" Margin="0,0,0,0" Orientation="Horizontal" HorizontalAlignment="Left">
                <Label Margin="5,0,0,0" HorizontalAlignment="Left" VerticalAlignment="Top" Content="Step 5:  Run Scan Every" FontFamily="Helvetica Neue" Foreground="#a0a0a0" HorizontalContentAlignment="Center" FontSize="11" FontWeight="Normal"/>
                <TextBox Margin="12,0,0,3" Text="10" Name="RunInterval" PreviewTextInput="RunInterval_PreviewTextInput" BorderBrush="#FF124B72" Background="Transparent"  BorderThickness="1"  Foreground="White" HorizontalContentAlignment="Right" HorizontalAlignment="Right" VerticalContentAlignment="Center" VerticalAlignment="Center" Width="50"  FontSize="11" Visibility="Visible"/>
                <Label Margin="5,0,0,0" HorizontalAlignment="Left" VerticalAlignment="Top" Content=" Minutes" FontFamily="Helvetica Neue" Foreground="#a0a0a0" HorizontalContentAlignment="Center" FontSize="11" FontWeight="Normal"/>
            </StackPanel>
            
            <!--<StackPanel Name="CloseButton" Grid.Row="8" Orientation="Horizontal" Visibility="Visible" HorizontalAlignment="Right"  Margin="0,0,8,0">
                <Label Content="CLOSE" Margin="5,0,0,0"  Padding="0,0" Background="Transparent" Foreground="White" HorizontalAlignment="Left" FontFamily="Helvetica Neue" MouseDown="" FontSize="11" VerticalAlignment="Center" FontWeight="Normal" Visibility="Visible"/>
            </StackPanel>-->

            <StackPanel Name="Row6" Grid.Row="6" Orientation="Horizontal" Visibility="Visible" HorizontalAlignment="Right"  Margin="0,0,8,0">
                <Label Content="Scan on Top at Notification:" Margin="5,0,0,0"  Padding="5,0" Background="Transparent" Foreground="#a0a0a0" HorizontalAlignment="Left" FontFamily="Helvetica Neue"  FontSize="11" VerticalAlignment="Center" FontWeight="Normal" Visibility="Visible"/>
                <CheckBox Name="OnTopCheckbox" VerticalAlignment="Center" Margin="0,0,0,0" Background="White"  Foreground="White" Checked="DefaultTitleCheckbox_Checked" Visibility="Visible" />
            </StackPanel>

            <StackPanel Name="Row5Universe" Grid.Row="5" Orientation="Horizontal" Visibility="Collapsed">
                <Label Name="Step5" Content="Step 5:  Name"  Padding="10,0" Background="Transparent" Foreground="White" HorizontalAlignment="Left" FontFamily="Helvetica Neue"  FontSize="11" VerticalAlignment="Center" FontWeight="Normal" Width="108" />
                <TextBox Name="UniverseTitle" Foreground="White" Width="385" TextChanged="ScanTitle_TextChanged" VerticalContentAlignment="Center"  BorderThickness="2"  BorderBrush="#FF124B72" Background="Black"/>
            </StackPanel>

            <StackPanel Grid.Row="6" Orientation="Horizontal" Visibility="Collapsed">
                <Label Content="Step 6:  Orientation:"  Padding="10,0" Background="Transparent" Foreground="White" HorizontalAlignment="Left" FontFamily="Helvetica Neue"  FontSize="11" VerticalAlignment="Center" FontWeight="Normal" Width="149" />
                <Label Content="Spreadsheet View"  Padding="5,0" Background="Transparent" Foreground="White" HorizontalAlignment="Left" FontFamily="Helvetica Neue"  FontSize="11" VerticalAlignment="Center" FontWeight="Normal" />
                <RadioButton Name="SpreadsheetRadio" VerticalAlignment="Center"/>
                <Label Content="Tree View"  Padding="15,0" Background="Transparent" Foreground="White" HorizontalAlignment="Left" FontFamily="Helvetica Neue"  FontSize="11" VerticalAlignment="Center" FontWeight="Normal" />
                <RadioButton Name="TreeRadio" VerticalAlignment="Center"/>
            </StackPanel>

            <StackPanel Grid.Row="10" Orientation="Horizontal">
                <Label Content="Save" Width="100" Padding="0" Margin="2"  MouseDown="Save_Click" HorizontalContentAlignment="Center"  VerticalContentAlignment="Center" FontSize="11"  Foreground="White"  FontFamily="Helvetica Neue" BorderBrush="Transparent" Background="#303030"  MouseEnter="Mouse_Enter" MouseLeave="Mouse_Leave"   Cursor="Hand" Visibility="Visible" />
                <Label Content="Cancel" Width="100" Padding="0" Margin="2" MouseDown="Cancel_Click" HorizontalContentAlignment="Center"  VerticalContentAlignment="Center" FontSize="11"  Foreground="White"  FontFamily="Helvetica Neue" BorderBrush="Transparent" Background="#303030" MouseEnter="Mouse_Enter" MouseLeave="Mouse_Leave"   Cursor="Hand" Visibility="Visible" />
                    <Label Content="Close" Width="100" Padding="0" Margin="2" MouseDown="CloseDialog_MouseDown" HorizontalContentAlignment="Center"  VerticalContentAlignment="Center" FontSize="11"  Foreground="White"  FontFamily="Helvetica Neue" BorderBrush="Transparent" Background="#303030" MouseEnter="Mouse_Enter" MouseLeave="Mouse_Leave"   Cursor="Hand" Visibility="Visible" />
                </StackPanel>
        </Grid>
    </Viewbox>
    </Border>
</Window>

